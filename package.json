{
  "name": "netbox-data-import",
  "private": true,
  "type": "module",
  "description": "JS test harness for the plugin's first-party browser assets",
  "license": "Apache-2.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "playwright test netbox_data_import/tests/browser/contact_candidate_modal.spec.js"
  },
  "engines": {
    "node": "^24.15.0 || >=26.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^30.0.1",
    "tom-select": "^2.4.3",
    "vitest": "^4.1.10"
  }
}
